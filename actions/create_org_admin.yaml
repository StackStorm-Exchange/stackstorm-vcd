---
  name: "create_org_admin"
  runner_type: "python-script"
  description: "Create an Organisation Admin Account"
  enabled: true
  entry_point: "create_org_admin.py"
  parameters:
    vcloud:
      type: "string"
      description: "Which vcloud to connect to"
      required: false
      position: 0
    data:
      type: "object"
      description: "Data structure for new organisation"
      required: true
      position: 1

