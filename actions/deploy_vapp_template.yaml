---
  name: "deploy_vapp_template"
  runner_type: "python-script"
  description: "Deploy VAPP template."
  enabled: true
  entry_point: "deploy_vapp_template.py"
  parameters:
    vcloud:
      type: "string"
      description: "Which vcloud to connect to"
      required: false
      position: 0
    data:
      type: "object"
      description: "Data structure for new vms"
      required: true
      position: 1

