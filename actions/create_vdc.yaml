---
  name: "create_vdc"
  runner_type: "python-script"
  description: "Create an VDC"
  enabled: true
  entry_point: "create_vdc.py"
  parameters:
    vcloud:
      type: "string"
      description: "Which vcloud to connect to"
      required: false
      position: 0
    data:
      type: "object"
      description: "Data structure for new organisation"
      required: True
      position: 1

