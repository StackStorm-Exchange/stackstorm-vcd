---
  name: "create_org"
  runner_type: "python-script"
  description: "Create an Organisation"
  enabled: true
  entry_point: "create_org.py"
  parameters:
    vcloud:
      type: "string"
      description: "Which vcloud to connect to"
      required: false
      position: 0
    data:
      type: "object"
      description: "Data structure for new organisation"
      required: true
      position: 1

